# MySQL connection string (assignment allows PostgreSQL or MySQL)
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/trello_clone"

# Frontend URL(s) for CORS — comma-separated if you have several (e.g. Render + local)
# FRONTEND_ORIGIN="https://your-app.onrender.com,http://localhost:5173"
FRONTEND_ORIGIN="http://localhost:5173"

# Optional (default 5000)
# PORT=5000
