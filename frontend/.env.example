# Optional in local dev: leave unset to use Vite proxy (same origin /api → localhost:5000).
# If you set it: use http://localhost:5000 only — do NOT add /api (that breaks requests).
# VITE_API_URL=http://localhost:5000
